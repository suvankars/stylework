#json.array!(@lists) do |list|
#  json.extract! list, :id, :ride_title, :ride_description, :rider_height, #:frame_size, :hourly_rental, :morning_rental, :evening_rental, :daily_rental, #:weekly_rental, :willing_to_deliver, :address, :city, :state, :pincode, :landmark
#  json.url list_url(list, format: :json)
#end

