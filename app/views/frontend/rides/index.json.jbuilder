#json.array!(@rides) do |ride|
#  json.extract! ride, :id, :ride_title, :ride_description, :rider_height, #:frame_size, :hourly_rental, :morning_rental, :evening_rental, :daily_rental, #:weekly_rental, :willing_to_deliver, :address, :city, :state, :pincode, :landmark
#  json.url ride_url(ride, format: :json)
#end

